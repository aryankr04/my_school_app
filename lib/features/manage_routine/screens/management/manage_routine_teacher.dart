import 'package:flutter/material.dart';

class ManageRoutineTeacher extends StatelessWidget {
  const ManageRoutineTeacher({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
